@model List<Post>

<div class="container-fluid">
    <div class="news-title">
        <h3>Новости</h3>
    </div>
    @{await Html.RenderPartialAsync("~/Views/User/Posts.cshtml", Model);}
</div>
